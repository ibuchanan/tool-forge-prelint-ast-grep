id: use-forge-variables-set-encrypted
language: Json
severity: info
message: >
  Name this script `forge:variables:set-encrypted` so forge variables set --encrypt wrappers are discoverable and composable.
note: >
  repo-init names Forge CLI wrapper scripts with a `forge:` namespace.
  `forge variables set --encrypt` is the separate `forge:variables:set-encrypted`
  convention, distinct from a plain `forge variables set` (named
  `forge:variables:set`).
rule:
  any:
    - all:
        - kind: pair
          regex: 'forge variables set\b'
          inside:
            stopBy: end
            kind: pair
            regex: '"scripts"\s*:'
        - regex: "--encrypt"
        - not:
            regex: '^"forge:variables:set-encrypted"\s*:'
    - all:
        - kind: string_content
          regex: "^@forge/(api|react|bridge|kvs|events|resolver)$"
          inside:
            stopBy: end
            kind: pair
            regex: '"dependencies"\s*:'
        - not:
            inside:
              stopBy: end
              kind: object
              has:
                kind: pair
                regex: '"forge:variables:set-encrypted"\s*:'
files:
  - "**/package.json"
