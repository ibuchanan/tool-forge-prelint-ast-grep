id: no-native-fetch-in-resolvers
valid:
  - "api.fetch(url)"
  - "api.asUser().fetch(url, { method: 'GET' })"
invalid:
  - "fetch(url)"
  - "fetch(url, { method: 'POST', body: JSON.stringify(data) })"
