id: lefthook-runs-generated-guards
snapshots:
  ? |
    pre-commit:
      commands:
        gitleaks:
          run: gitleaks protect --staged
        lint:
          run: npm run lint
        format:
          run: npm run format:check
  : labels:
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
      style: primary
      start: 0
      end: 153
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
      style: secondary
      start: 0
      end: 153
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
      style: secondary
      start: 0
      end: 153
  ? |
    pre-commit:
      commands:
        gitleaks:
          run: gitleaks protect --staged
        lint:
          run: npm run lint
        format:
          run: npm run format:check
        typecheck:
          run: npm run typecheck
    pre-push:
      commands:
        build:
          run: npm run build
  : labels:
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
            typecheck:
              run: npm run typecheck
        pre-push:
          commands:
            build:
              run: npm run build
      style: primary
      start: 0
      end: 255
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
            typecheck:
              run: npm run typecheck
        pre-push:
          commands:
            build:
              run: npm run build
      style: secondary
      start: 0
      end: 255
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
            lint:
              run: npm run lint
            format:
              run: npm run format:check
            typecheck:
              run: npm run typecheck
        pre-push:
          commands:
            build:
              run: npm run build
      style: secondary
      start: 0
      end: 255
