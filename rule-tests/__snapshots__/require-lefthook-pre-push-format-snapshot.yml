id: require-lefthook-pre-push-format
snapshots:
  ? |
    pre-commit:
      commands:
        gitleaks:
          run: gitleaks protect --staged
  : labels:
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
      style: primary
      start: 0
      end: 75
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
      style: secondary
      start: 0
      end: 75
    - source: |
        pre-commit:
          commands:
            gitleaks:
              run: gitleaks protect --staged
      style: secondary
      start: 0
      end: 75
  ? |
    pre-push:
      commands:
        typecheck:
          run: npm run typecheck
        lint:
          run: npm run lint
        test:
          run: npm run test
  : labels:
    - source: |
        pre-push:
          commands:
            typecheck:
              run: npm run typecheck
            lint:
              run: npm run lint
            test:
              run: npm run test
      style: primary
      start: 0
      end: 134
    - source: |
        pre-push:
          commands:
            typecheck:
              run: npm run typecheck
            lint:
              run: npm run lint
            test:
              run: npm run test
      style: secondary
      start: 0
      end: 134
    - source: |
        pre-push:
          commands:
            typecheck:
              run: npm run typecheck
            lint:
              run: npm run lint
            test:
              run: npm run test
      style: secondary
      start: 0
      end: 134
