id: use-node-24
valid:
  # Already pinned to Node.js 24.x
  - |
    {
      "dependencies": {
        "@forge/api": "^4.0.0"
      },
      "engines": {
        "node": "24.x"
      }
    }
invalid:
  # engines missing entirely
  - |
    {
      "dependencies": {
        "@forge/api": "^4.0.0"
      }
    }
  # Pinned to an older Node.js version
  - |
    {
      "dependencies": {
        "@forge/api": "^4.0.0"
      },
      "engines": {
        "node": "18.x"
      }
    }
