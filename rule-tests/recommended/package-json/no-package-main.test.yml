id: no-package-main
valid:
  # Not a Forge app
  - |
    {
      "main": "dist/index.js",
      "dependencies": {
        "express": "^4.18.0"
      }
    }
  - |
    {
      "dependencies": {
        "@forge/api": "^8.0"
      }
    }
invalid:
  - |
    {
      "main": "dist/index.mjs",
      "dependencies": {
        "@forge/api": "^8.0"
      }
    }
